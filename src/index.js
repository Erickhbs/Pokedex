import React from 'react';
import ReactDOM from 'react-dom/client';
import Busca from './busca/consulta-api';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <div>
        <Busca />
    </div>
);
